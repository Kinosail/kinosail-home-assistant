repos:
  - repo: local
    hooks:
      - id: quality-gate
        name: Kinosail local lint (full checks run in GitHub Actions)
        entry: make lint
        language: system
        pass_filenames: false
        always_run: true
